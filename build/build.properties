# The directory containing the sources.
impl.path=${basedir}

# The directory to place build distribution components (the build output) into.
dist.path=${impl.path}/target

# The default directory to place release archives
release.dir=${dist.path}

deployable.prefix=mavenus

# The default devstream name to use in naming a release
devstream.name=trunk

